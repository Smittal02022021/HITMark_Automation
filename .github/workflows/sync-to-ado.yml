name: Sync GitHub to Azure DevOps

on:
  push:
    branches:
      - main

jobs:
  sync:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Configure Git
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "github-actions@github.com"

      - name: Add Azure DevOps Remote
        env:
          AZURE_DEVOPS_PAT: ${{ secrets.PERSONAL_AZURE_DEVOPS_PAT }}
          AZURE_DEVOPS_REPO: ${{ secrets.PERSONAL_AZURE_DEVOPS_REPO }}
        run: |
          git remote add ado https://:${PERSONAL_AZURE_DEVOPS_PAT}@${PERSONAL_AZURE_DEVOPS_REPO#https://}

      - name: Push to Azure DevOps
        run: |
          git push ado main